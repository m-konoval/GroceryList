export enum EStatus {
  have = "have",
  out = "out",
}

export const EPriorityOptions = [1, 2, 3, 4, 5];

export const EStatusOptions = [EStatus.have, EStatus.out];

export enum EScreens {
  List = "ScreenList",
  Details = "ScreenDetails",
}

export enum EStorageKeys {
  list = "gloceryList",
}
